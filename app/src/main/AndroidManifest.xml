<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bw.bwallet">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".home.MainActivity"
            android:configChanges="locale" >


        </activity>
        <activity android:name=".createwallet.CreateWalletActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".createwallet.ImportWalletActivity" />
        <activity android:name=".createwallet.BackupWalletActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".user.WalletDetailInfoActivity" />
        <activity android:name=".user.ChangePwdActivity" />
        <activity android:name=".transfer.TransferOutActivity" />
        <activity android:name=".transfer.TransferPreviewActivity" />
        <activity android:name=".transfer.TransferRecordActivity"></activity>
    </application>

</manifest>